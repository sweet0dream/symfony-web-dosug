<div class="row justify-content-center position-relative g-2 m-0 px-1 pb-4 premium">
    {% for item in items %}
        <div class="col-{% if is_mobile() %}6{% else %}2{% endif %}">
            {% include 'section/render/premium.html.twig' with {item} %}
        </div>
    {% endfor %}
</div>
