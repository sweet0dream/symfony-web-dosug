<div class="row justify-content-center g-2 premium">
    {% for item in items %}
        <div class="col-{% if is_mobile() %}6{% else %}2{% endif %}">
            {% include 'section/render/premium.html.twig' with {item} %}
        </div>
    {% endfor %}
</div>
