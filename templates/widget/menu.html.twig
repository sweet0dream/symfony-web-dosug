<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">{{ site_name }}</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarText">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                {% for keyMenu, valueMenu in menu %}
                    {% if keyMenu != 'main' %}
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{{ url('page_type', {'type': valueMenu['slug']}) }}">{{ valueMenu['item_menu'] }}</a>
                    </li>
                    {% endif %}
                {% endfor %}
            </ul>
            <div class="navbar-text">
                <div class="dropdown">
                    <button class="btn btn-outline-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Размещение
                    </button>
                    <ul class="dropdown-menu dropdown-menu-dark dropdown-menu-end">
                        <li><a class="dropdown-item" href="/user/login">Вход в личный кабинет</a></li>
                        <li><a class="dropdown-item" href="/user/regin">Регистрация рекламодателя</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="#">Стоимость рекламы</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</nav>